import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ruth.and.jerry',
  appName: 'ruth-and-jerry',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
